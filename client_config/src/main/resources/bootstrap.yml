server:
  port: 7002
spring:
  application:
    name: myconfig
  cloud:
      config:
        uri: http://localhost:7001  #配置中心的请求路径
        profile: test
        lable: master
management:
  endpoints:
    web:
      exposure:
        include: '*'




